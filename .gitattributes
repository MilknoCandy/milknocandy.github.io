# 强制所有文件使用 LF 换行符，防止不同系统间的差异
* text=auto eol=lf

# 核心：将 CSS 和 JS 视为二进制文件
# 这样 Git 在上传时绝不会改动里面的任何一个字节，从而保证哈希值不变
*.css binary
*.js binary

# 常见的图片资源也设为二进制
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary